# Crypto-SIP
A recurring-buy (dollar-cost-averaging) contract. A user locks funds upfront for N intervals; anyone (a keeper bot, or you manually) can trigger each interval's swap once it's due, earning a small reward for doing so.
